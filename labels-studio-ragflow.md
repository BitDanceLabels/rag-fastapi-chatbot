git@github.com:BitDanceLabels/label-studio.git 
ngữ cảnh nhận diện
=> khi nào nhận diện
=> khi nào thì labels và mapping vô database => 
# 

mapping code labelsing vô hệ thống => để embedding anything => 
update chunking 
update image embedding và VLM 
OCR test 
#  Cụm ngữ cảnh và các đối tượng => mapping các lớp 
# chunking và LLM output 

# LLM labels tự động
# human labels 
# điểm uy tín và ưu tiên 

# đánh nhãn lại => update lại chunking =>> vậy là cũng phải videcode bắt chước thêm 
cơ chế re - embedding vào chunking và vào post PUT update 

các models tự động multi database  _ google search APi bổ sung 

#
